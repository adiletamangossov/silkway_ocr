# secrets: never commit real credentials
.env

# local stand-in databases for the validation / decision-sink / queue tests+stubs
user_ids.db
ocr_decisions.db
manual_queue.db

# decision log: real transcripts + ids, treat as data, never commit
decisions.jsonl

# preprocess A/B experiment log: same real transcripts + ids, data only
preprocess_ab.jsonl

# sample courier photos: real client member_ids + addresses (PII), never commit
*.jpg
*.jpeg
*.png

# db-built eval sets: real photos + a manifest of real member_ids (PII), data only
eval_samples/db/
eval_samples/db_random/

# jetbrains ide
.idea/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
