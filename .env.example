# Template for the PostgreSQL credentials used by validation.PostgresUserIDStore.
# Copy this file to .env and fill in real values. Safe to commit (no secrets).
DB_HOST=
DB_PORT=4444
DB_USER=
DB_PASSWORD=
DB_NAME=
